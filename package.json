{
  "name": "sports-scores-site",
  "version": "1.0.0",
  "description": "Fetch MLB, NBA, Soccer scores and update GitHub Pages",
  "main": "index.js",
  "scripts": {
    "fetch": "node scripts/fetch_mlb_scores.js && node scripts/fetch_nba_scores.js && node scripts/fetch_soccer_scores.js"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
